/**
 * The house motion curve — decisive ease-out, no bounce.
 * Lives in its own module so component files export only components
 * (keeps React Fast Refresh working).
 */
export const EASE = [0.22, 1, 0.36, 1];
