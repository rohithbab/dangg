.env
.env.*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Screenshots and scratch captures at the repo root only — NOT real assets.
# A blanket *.png once silently excluded src/assets/brand/*.png, so the
# Docker build failed with UNRESOLVED_IMPORT while local builds passed.
/*.png
.playwright-mcp/
